@typeparam TItem

<tr>
    @ChildContent(Item)
</tr>

@code {

    /// <summary>
    ///     Gets or sets the context for the row.
    /// </summary>
    [Parameter]
    public TItem Item { get; set; }

    /// <summary>
    ///     Gets or sets the contents of the grid row.
    /// </summary>
    [Parameter]
    public RenderFragment<TItem> ChildContent { get; set; }
}