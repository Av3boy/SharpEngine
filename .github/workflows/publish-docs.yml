name: Publish Docs

on:
  push:
    branches:
      - test
  pull_request:
    branches:
      - test

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: echo "Hello from the test branch 👋"
